"""
Database models (tables) will live here.

In Stage 2, we will add our first real table: the User model
(for registration and login). In later stages we'll add Income,
Expense, Budget, and SavingsGoal tables here too.

For Stage 1, this file is intentionally empty except for this
explanation - we just want to confirm the database connection works.
"""

from extensions import db  # noqa: F401  (kept here so Stage 2 can use it immediately)
